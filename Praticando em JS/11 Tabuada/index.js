const N = 10;
let multiplicador = 0;
const multiplicação = N * multiplicador;
let i = 0;
while (i <= N) {
  console.log(`${N} x ${multiplicador} = ${multiplicador * N}`);
  multiplicador += 1;
  i += 1;
}
